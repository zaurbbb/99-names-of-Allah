import React from 'react';

import PlayBtn from './playButton.svg'

const PlayIcon = () => {
    return (
        <img
            src={PlayBtn}
            alt='icon'
        />
    );
};

export default PlayIcon;